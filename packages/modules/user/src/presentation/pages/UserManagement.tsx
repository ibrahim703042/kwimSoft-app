/**
 * UserManagement — Renders the tabbed view (same pattern as HR "Gestion RH").
 * Used by HrShell as the "Gestion Utilisateurs" content.
 */
import UserTabbedView from "../UserTabbedView";

export default function UserManagement() {
  return <UserTabbedView />;
}
