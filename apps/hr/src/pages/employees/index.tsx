/**
 * Employees module — re-exports the main employee list/form page.
 * Implementation uses employee.schema.ts and @kwim/core (CrudPage, CrudForm, TabbedForm).
 */
export { default } from "./EmployeePage";
